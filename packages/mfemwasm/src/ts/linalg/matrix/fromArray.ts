/**
 * Create dense matrix from 2D array
 * @param data - 2D array data
 * @param rowMajor - Whether data is row-major (default true)
 * @returns DenseMatrix instance
 */
export function fromArray(data: number[][], rowMajor?: boolean): unknown {
  throw new Error('Not implemented');
}
