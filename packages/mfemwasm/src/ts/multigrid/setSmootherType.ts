/**
 * Set smoother type for multigrid
 * @param mg - Multigrid instance
 * @param type - Smoother type
 */
export function setSmootherType(
  mg: unknown,
  type: 'jacobi' | 'gauss-seidel' | 'chebyshev'
): void {
  throw new Error('Not implemented');
}
