/**
 * Get integration rule for geometry and order
 * @param geomType - Geometry type (segment, triangle, quad, tet, hex, etc.)
 * @param order - Integration order
 * @returns IntegrationRule instance
 */
export function get(geomType: string, order: number): unknown {
  throw new Error('Not implemented');
}
