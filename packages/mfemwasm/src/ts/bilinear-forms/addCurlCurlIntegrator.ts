/**
 * Add curl-curl integrator (∇×u, ∇×v) to bilinear form
 * @param blf - BilinearForm instance
 * @param coeff - Optional scalar coefficient
 */
export function addCurlCurlIntegrator(blf: unknown, coeff?: unknown): void {
  throw new Error('Not implemented');
}
