/**
 * Add mass integrator (u, v) to bilinear form
 * @param blf - BilinearForm instance
 * @param coeff - Optional scalar coefficient
 */
export function addMassIntegrator(blf: unknown, coeff?: unknown): void {
  throw new Error('Not implemented');
}
