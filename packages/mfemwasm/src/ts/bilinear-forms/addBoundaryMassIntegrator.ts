/**
 * Add boundary mass integrator on specified boundaries
 * @param blf - BilinearForm instance
 * @param coeff - Optional scalar coefficient
 * @param bdrAttrs - Optional boundary attributes
 */
export function addBoundaryMassIntegrator(
  blf: unknown,
  coeff?: unknown,
  bdrAttrs?: number[]
): void {
  throw new Error('Not implemented');
}
