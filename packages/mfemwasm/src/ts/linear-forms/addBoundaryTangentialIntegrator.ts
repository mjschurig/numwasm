/**
 * Add boundary tangential integrator (g·t, v) to linear form
 * @param lf - LinearForm instance
 * @param coeff - Vector coefficient
 * @param bdrAttrs - Optional boundary attributes
 */
export function addBoundaryTangentialIntegrator(
  lf: unknown,
  coeff: unknown,
  bdrAttrs?: number[]
): void {
  throw new Error('Not implemented');
}
