/**
 * Add boundary flux integrator to linear form
 * @param lf - LinearForm instance
 * @param coeff - Flux coefficient
 * @param bdrAttrs - Optional boundary attributes
 */
export function addBoundaryFluxIntegrator(
  lf: unknown,
  coeff: unknown,
  bdrAttrs?: number[]
): void {
  throw new Error('Not implemented');
}
