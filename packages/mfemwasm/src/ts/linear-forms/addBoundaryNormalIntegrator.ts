/**
 * Add boundary normal integrator (g·n, v) to linear form
 * @param lf - LinearForm instance
 * @param coeff - Vector coefficient
 * @param bdrAttrs - Optional boundary attributes
 */
export function addBoundaryNormalIntegrator(
  lf: unknown,
  coeff: unknown,
  bdrAttrs?: number[]
): void {
  throw new Error('Not implemented');
}
