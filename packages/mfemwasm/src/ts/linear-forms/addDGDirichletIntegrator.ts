/**
 * Add DG Dirichlet boundary integrator
 * @param lf - LinearForm instance
 * @param coeff - Dirichlet value coefficient
 * @param sigma - Symmetry parameter
 * @param kappa - Penalty parameter
 */
export function addDGDirichletIntegrator(
  lf: unknown,
  coeff: unknown,
  sigma: number,
  kappa: number
): void {
  throw new Error('Not implemented');
}
