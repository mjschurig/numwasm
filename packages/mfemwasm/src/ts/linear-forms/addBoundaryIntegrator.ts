/**
 * Add boundary integrator (g, v) to linear form
 * @param lf - LinearForm instance
 * @param coeff - Boundary source coefficient
 * @param bdrAttrs - Optional boundary attributes
 */
export function addBoundaryIntegrator(
  lf: unknown,
  coeff: unknown,
  bdrAttrs?: number[]
): void {
  throw new Error('Not implemented');
}
