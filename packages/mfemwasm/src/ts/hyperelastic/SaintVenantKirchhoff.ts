/**
 * Create Saint Venant-Kirchhoff hyperelastic model
 * @param lambda - Lamé first parameter
 * @param mu - Lamé second parameter (shear modulus)
 * @returns SaintVenantKirchhoff instance
 */
export function create(lambda: number, mu: number): unknown {
  throw new Error('Not implemented');
}
